const getters = {
  // 其他getter...
  isCollapse: state => state.app.isCollapse,
  menuPosition: state => state.app.menuPosition,
  routeLoading: state => state.app.routeLoading
}

export default getters
